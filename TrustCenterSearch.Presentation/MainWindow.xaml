<Window x:Class="TrustCenterSearch.Presentation.MainWindow"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converter="clr-namespace:TrustCenterSearch.Presentation.Converter"
        mc:Ignorable="d"
        DataContext="{StaticResource ViewModel}"
        Title="TrustCenterExecuteSearchBarInput" Height="550" Width="1100">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadDataCommand}"></i:InvokeCommandAction>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Styles.xaml">
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
            <converter:HighlightConverter x:Key="HighlightConverter" />
        </ResourceDictionary>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBox Name ="SearchBar"  Grid.Column="1" Grid.Row="0"  Margin="10"  Padding="3" Width="auto" Height="auto">
            <TextBox.Text>
                <Binding Path="SearchBarInput" Delay="500" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay"></Binding>
            </TextBox.Text>
        </TextBox>
        <TextBlock Grid.Column="1" Grid.Row="0" Margin="15,10,10,10" Padding="3" FontStyle="Italic" Foreground="Gray" Text="" IsHitTestVisible="False" />

        <Border Grid.Column="1" Grid.Row="1" Margin="10"  Background="White" BorderBrush="Black" BorderThickness="1">
            <ItemsControl ItemsSource="{Binding Path=CertificatesCollectionView}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Height="145" Width="830">
                            <Border Margin="5,5,5,5" BorderBrush="Black" BorderThickness="1" CornerRadius="8,8,8,8">
                                <WrapPanel Margin="0,0,0,0">
                                    <StackPanel>
                                        <WrapPanel>
                                            <Label Content="Subject:" Foreground="Gray" FontSize="16"></Label>
                                            <Label Content="{Binding Subject}" Width="700" FontSize="16" Foreground="{Binding Subject, Converter={StaticResource HighlightConverter}, ConverterParameter={x:Reference SearchBar}}"></Label>
                                        </WrapPanel>
                                        <WrapPanel>
                                            <Label Content="Issuer:" Foreground="Gray" FontSize="16"></Label>
                                            <Label Content="{Binding Issuer}" Width="auto" FontSize="16" Foreground="{Binding Issuer, Converter={StaticResource HighlightConverter}, ConverterParameter={x:Reference SearchBar}}"></Label>
                                        </WrapPanel>
                                        <WrapPanel Margin="0,0,0,0">
                                            <Label Content="Not After:" Foreground="Gray" FontSize="16" Width="auto"></Label>
                                            <Label Content="{Binding NotAfter}" Height="auto" Width="auto" FontSize="16" Foreground="{Binding NotAfter, Converter={StaticResource HighlightConverter}, ConverterParameter={x:Reference SearchBar}}"></Label>
                                            <Label Content="Not Before:" Foreground="Gray" FontSize="16"></Label>
                                            <Label Content="{Binding NotBefore}" Height="auto" Width="auto" FontSize="16" Foreground="{Binding NotBefore, Converter={StaticResource HighlightConverter}, ConverterParameter={x:Reference SearchBar}}"  Margin="20,0,0,0"></Label>
                                            <Label Content="Serialnumber:" Foreground="Gray" FontSize="16" Margin="20,0,0,0"></Label>
                                            <Label Content="{Binding SerialNumber}" Height="auto" Width="auto" FontSize="16" Foreground="{Binding SerialNumber, Converter={StaticResource HighlightConverter}, ConverterParameter={x:Reference SearchBar}}"></Label>
                                        </WrapPanel>
                                        <WrapPanel>
                                            <Label Content="Thumbprint:" Foreground="Gray" FontSize="16"></Label>
                                            <Label Content="{Binding Thumbprint}" Height="auto" Width="auto" FontSize="16" Foreground="{Binding Thumbprint, Converter={StaticResource HighlightConverter}, ConverterParameter={x:Reference SearchBar}}"></Label>
                                        </WrapPanel>
                                    </StackPanel>
                                </WrapPanel>
                            </Border>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel IsVirtualizing="True" VirtualizationMode="Recycling"></VirtualizingStackPanel>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.Template>
                    <ControlTemplate TargetType="ItemsControl">
                        <ScrollViewer CanContentScroll="True" 
                              Focusable="False">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </ControlTemplate>
                </ItemsControl.Template>

            </ItemsControl>
        </Border>

        <!-- Menu -->
        <Grid Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Margin="0,10,10,10" Background="LightGray">
            <Grid.RowDefinitions>
                <RowDefinition Height="160"></RowDefinition>
                <RowDefinition Height="40"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <!-- AddTrustCenter -->
            <Grid Grid.Column="0" Grid.Row="0">
                <StackPanel>
                    <Label Content="Add TrustCenter" FontSize="20" Margin ="10,0,0,0"></Label>
                    <WrapPanel Margin="5">
                        <Label Width="45" Content="Name" Margin ="10,0,0,0"></Label>
                        <TextBox Padding="3" Width="200" Height="auto" TextChanged="TextBox_TextChanged" Text="{Binding AddTrustCenterName, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                    </WrapPanel>
                    <WrapPanel Margin="5">
                        <Label Width="45" Content="URL" Margin ="10,0,0,0"></Label>
                        <TextBox Padding="3" Width="200" Height="auto" Text="{Binding AddTrustCenterUrl, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                    </WrapPanel>
                    <Button Style="{StaticResource AddButtonStyle}" Command="{Binding AddTrustCenterButtonCommand}"></Button>
                </StackPanel>
            </Grid>
            <!-- AddTrustCenter -->

            <!-- TrustCenterHistory -->
            <Grid Grid.Column="0" Grid.Row="1">
                <Label Content="Trust Center History" FontSize="20" Margin ="10,0,0,0"></Label>
            </Grid>

            <Grid Grid.Row="2">
                <ScrollViewer>
                    <StackPanel>
                        <ItemsControl ItemsSource="{Binding Path=TrustCenterHistory}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel>
                                    </StackPanel>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <WrapPanel Margin="10,5,5,5">
                                        <Button Background ="LightGray" Height="20" Width="20" BorderThickness="0">
                                            <Image VerticalAlignment ="Center" Source ="/Resources/Images/add.png"></Image>
                                        </Button>
                                        <Label Width="200" Background="LightGray"></Label>
                                        <Button Command="{Binding Source={StaticResource ViewModel}, Path=DeleteTrustCenterFromHistoryCommand}" Background ="LightGray" Height="20" Width="20" BorderThickness="0">
                                            <Image VerticalAlignment="Center" Source ="/Resources/Images/binBlack.png"></Image>
                                        </Button>
                                    </WrapPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <!-- TrustCenterHistory -->

        </Grid>
        <!-- Menu -->

    </Grid>
</Window>
